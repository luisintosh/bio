# luism.dev

Personal site for Luis Mendieta — Vite + React, deployed to GitHub Pages.

## Scripts

```bash
bun install
bun run dev
bun run build
```

Open the local URL Vite prints (often `http://127.0.0.1:5173/`).
